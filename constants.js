const CSV_FILE_PATH = "_.csv";

const JSON_FILE_NAME = "_.json";

const OUTPUT_DIRECTORY = "./output_sql_files";

const MAX_INSERTS_PER_FILE = 2000;

const DATABASE = "_";
const TABLE_NAME = "_";

const SQL_OUTPUT_FILE_NAME = "_";

module.exports = {
  CSV_FILE_PATH,
  JSON_FILE_NAME,
  OUTPUT_DIRECTORY,
  MAX_INSERTS_PER_FILE,
  DATABASE,
  TABLE_NAME,
  SQL_OUTPUT_FILE_NAME,
};
